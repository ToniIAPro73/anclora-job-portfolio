#!/usr/bin/env node
// No TypeScript in this project (plain HTML/CSS/JS, per stack decision from
// the web-portfolio-builder AOS skill: browser_only, no build step).
console.log("typecheck: N/A — project is plain HTML/CSS/JS, no TypeScript sources");
process.exit(0);
